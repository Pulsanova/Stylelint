{
    "private": true,
    "scripts": {
        "lint": "eslint .",
        "bump-all": "lerna version --no-git-tag-version --no-push",
        "publish-all": "lerna publish from-package"
    },
    "devDependencies": {
        "@pulsanova/eslint-config-base": "^2.0.0",
        "eslint": "~8.3.0",
        "lerna": "^4.0.0"
    }
}
