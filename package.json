{
    "private": true,
    "scripts": {
        "lint": "eslint .",
        "bump-all": "lerna version --no-git-tag-version --no-push",
        "publish-all": "lerna publish from-package"
    },
    "devDependencies": {
        "@pulsanova/eslint-config-base": "^0.3.1",
        "eslint": "~7.4.0",
        "lerna": "^3.22.1"
    }
}
