{
    "name": "@pulsanova/stylelint",
    "type": "module",
    "private": true,
    "scripts": {
        "lint": "eslint .",
        "find-new-rules": "npm run find-new-rules --workspaces",
        "bump-all": "npx lerna version --no-git-tag-version --no-push",
        "publish-all": "npx lerna publish from-package"
    },
    "workspaces": [
        "packages/*"
    ],
    "devDependencies": {
        "@pulsanova/eslint-config-node": "^3.0.1",
        "eslint": "~9.33.0",
        "lerna": "^8.2.3"
    },
    "engines": {
        "node": "^22"
    }
}
