{
    "private": true,
    "scripts": {
        "lint": "eslint .",
        "bump-all": "lerna version --no-git-tag-version --no-push",
        "publish-all": "lerna publish from-package"
    },
    "devDependencies": {
        "@pulsanova/eslint-config-base": "^1.0.0",
        "eslint": "~7.6.0",
        "lerna": "^3.22.1"
    }
}
